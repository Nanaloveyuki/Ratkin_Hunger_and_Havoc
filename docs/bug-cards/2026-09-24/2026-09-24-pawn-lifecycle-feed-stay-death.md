# 2026-09-24 进食、期限和死亡没有闭合生命周期

| 字段 | 写什么 |
| --- | --- |
| 复现 | 0.1.0 新游戏，地图上来客吃到饱，或右键投喂。再收留、雇佣或招募，等到短工、长工或招募期限结束。最后让来客死亡 |
| 期望 / 实际 | 期望实际吃饱或右键投喂后进入 `Fed` 并写下 `leaveAfterGameTick`；短工、长工、招募到期且不再倒地后清掉玩家派系、进入 `Leaving` 并拆掉访客 Lord；死亡进入 `Dead` 并清掉 duty 和访客 Lord。实际 `TryComplete` 没有任何调用方，右键投喂只改生命周期，到期不拆 Lord，死亡不改生命周期，`NotifyDead` 没有调用方 |
| 版本 | 0.1.0 |
| 级别 | S1 |
| 根因 | 原版进食完成和右键投喂没有走同一条喂食完成。`Tick` 到期只改生命周期。死亡没有接到 `SetLifecycle(Dead)` 和已有的访客清理 |
| 最小修复 | 原版 `FinalizeIngest` 完成后调用喂食完成。右键投喂也走这一条，不再直接改生命周期。到期拆掉该 pawn 的 Lord、duty 和访客 Job，再进入 `Leaving`。`Pawn.Kill` 之后把仍活跃的来源标成 `Dead` 并调用 `NotifyDead` |
| 明确不做 | 不改选择信投喂。它仍是 `WaitsForFood`，只等玩家交食物，不直接标 `Fed`。不改啃食营养和伤口。不改吃饱后的停留天数和倒地暂停。招募期限本来就用短工天数，不改。不改 Carry、无食物等待、TailBite、Leash、N007 和事件目标保存。不新增存档键。不给死亡补玩家信件 |
| Language | 无 |
| 存档 | 无新键。招募期限仍写入已有 `stayKind` 与 `leaveAfterGameTick` |
| 归属表 | 否 |
| 回归 | 招募期限仍用短工天数，到期才离场，倒地不离场。源码契约覆盖原版进食和右键调用喂食完成，选择信仍走 `WaitsForFood`，到期清理 Lord，死亡补丁调用 `NotifyDead` |
| 后续债 | 无 Lord 的到期来客只清 duty 和 Job。真正走向地图出口仍由已有离场 duty 负责。同 Lord 的其它活着成员不因这一只死亡被拆掉 |
