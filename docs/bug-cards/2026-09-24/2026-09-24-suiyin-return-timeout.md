# 2026-09-24 穗音访客无法返回时永久等待

| 字段 | 写什么 |
| --- | --- |
| 复现 | 0.1.0。触发需要穗音持续观察的访客事件。使相关 Pawn 离开原地图、转移或无法再由 `FindPawn` 定位，等待观察期限结束并继续运行游戏 |
| 期望 / 实际 | 期望在有限观察期内等待，确认回归、死亡、被玩家接收或永久缺失后收束案件。实际 Pawn 无法带回时案件可能持续等待，重复占用叙事状态 |
| 版本 | 0.1.0 |
| 级别 | S1 |
| 根因 | 观察逻辑缺少统一的“无法定位”起始时间和终止条件。无法区分暂时离图、死亡、被接收和永久缺失，现有等待状态没有可靠超时收束 |
| 最小修复 | 复用现有案件观察周期和 `MissingSince`。首次无法定位时记录时间；期限内允许 Pawn 回归；确认死亡、加入玩家派系或超过期限后分别进入已有结果或 `Missing` 终态。终态停止重复通知和副作用，不生成替身 Pawn |
| Language | 无 |
| 存档 | 可能需要使用或新增案件的 `MissingSince` 字段。若新增存档键，需同步 `docs/save-ownership.md`；不得把 Pawn 的 `thingIDNumber` 当作永久跨存档身份 |
| 归属表 | 若新增持久化字段：是；登记 `MissingSince` 的 Remove/Replace 动作 |
| 回归 | Pawn 在期限内回到原地图时案件继续；Pawn 死亡进入死亡结果；Pawn 被玩家接收进入已处理结果；Pawn 永久缺失并超过观察期限后案件关闭；读档保留缺失起始时间；终态不重复发信或重复改信任 |
| 后续债 | 评估是否需要稳定关系 ID 替代单独使用 `thingIDNumber` |
