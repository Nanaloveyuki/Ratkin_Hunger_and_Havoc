# 2026-09-24 穗音身份决定缺少玩家入口

| 字段 | 写什么 |
| --- | --- |
| 复现 | 0.1.0。达到穗音身份相关条件，运行身份检查并观察身份信、结局或叙事菜单 |
| 期望 / 实际 | 期望：若身份公开或拒绝是玩家决策，应出现一次可存档的选择，并让选择影响后续状态；若该内容尚未形成玩法，则不应向玩家承诺选择。实际身份状态可以直接标记并发信，没有稳定的接受/拒绝入口 |
| 版本 | 0.1.0 |
| 级别 | S2（若已发出明确的可选择文案则升为 S1） |
| 根因 | `MarkIdentity`、身份等级和结局展示已有数据路径，但没有完整的玩家选择状态、选择信和结果处理。当前实现把“提供身份信息”和“玩家决定”混为一体 |
| 最小修复 | 先做消融：暂时关闭 N-010/R-01 的不可交互入口与结局依赖，确认身份线是否改变玩家决策。若确认保留，复用现有身份状态和 deadline 机制，增加一次性存档选择信，至少实现接受与拒绝；每个结果只能结算一次，并真正影响信任或对应结局。不要新增并行状态机 |
| Language | 若实现选择信：新增或修改的中文与英文 Keyed 必须成对，前缀使用 `RHAH_` |
| 存档 | 若新增接受/拒绝/期限状态：是。冻结前可破坏性重建，但必须记录旧键、新键、默认值和失败处理 |
| 归属表 | 若新增持久化状态：是；登记 Remove/Replace 动作 |
| 回归 | 未实现前不再显示会误导玩家的选择承诺。实现后：只生成一封选择信；接受和拒绝各只能执行一次；读档不重复结算；选择影响预期信任或结局；关闭穗音或切换叙事者时行为符合设置 |
| 后续债 | 先通过消融确认 N-010/R-01 是否具有足够玩法价值，再决定实现或删除 |
