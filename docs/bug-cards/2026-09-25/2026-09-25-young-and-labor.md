# 2026-09-25 鼠蛋事件没有幼年，待产没有怀孕，分妈没有伤口

| 字段 | 写什么 |
| --- | --- |
| 复现 | 触发只含幼年的事件，如 `I-002`、`I-003`、`I-007`、`I-009`、`I-013`、`I-032`、`I-033`。再触发 `I-029` 或 `I-044` 待产流民。对照 `I-001` 这类大批事件 |
| 期望 / 实际 | 期望鼠蛋事件生成幼年鼠族。`I-003` 第一名是成年雌性，缺左耳、右腿和鼠尾，倒地且失血至少 0.7，其余是 3 到 6.9 岁的幼年。`I-004` 是 1 名成年母亲加 4 名不足 3 岁的幼年。待产流民是成年雌性，进入后处于产程。实际这些事件整批按成年角色生成，年龄被丢掉，没有幼年、没有伤口、也没有怀孕。大批事件里的幼年角色仍能生成 |
| 版本 | 0.1.0 |
| 级别 | S1 |
| 根因 | `RHAH_VisitorRules.KeepsAge` 把 `Mother` 和 `BeggarMother` 当成已指定年龄。事件没有传入年龄，`GenerationAge` 返回空，`StageFor` 落到成年。专用鼠蛋事件又把整批角色写成 `Mother`、`Beggar`、`Thief`、`Wild` 或 `Refugee`，没有拆成幼年角色。`I-029` 与 `I-044` 只生成 `Beggar`，没有怀孕，也没有进入产程。`I-003` 没有拆掉左耳、右腿和鼠尾，也没有倒地和失血 |
| 最小修复 | 幼年角色在未指定年龄时用各自的幼年区间。`I-002`、`I-007`、`I-009`、`I-013`、`I-032`、`I-033`、`I-037`、`I-041`、`I-046`、`I-047` 整批用对应幼年角色。`I-003` 第一名是雌性 `Mother` 并带伤口，其余是 `RatkinYoung`。`I-004` 第一名是 `BeggarMother`，其余是 `BeggarChild`。`I-029` 与 `I-044` 是雌性 `BeggarMother`，生成后进入产程。大批混龄事件保持原随机比例 |
| 明确不做 | 不恢复旧标记 Hediff。不改大批事件的成年与幼年比例。不给分妈的幼年拆随机部位。不改牵引、选择信和离场 |
| Language | 无 |
| 存档 | 无 |
| 归属表 | 否 |
| 回归 | 未指定年龄的幼年角色落在对应区间，母亲仍为空。上述事件的角色拆分与旧人数一致。待产请求标记为进入产程。分妈母亲请求包含三处缺失部位 |
| 后续债 | 无 |
